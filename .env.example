POSTGRES_DB=app_db
POSTGRES_USER=app_user
POSTGRES_PASSWORD=change_me
DATABASE_URL=postgresql+asyncpg://app_user:change_me@postgres:5432/app_db

REDIS_URL=redis://redis:6379/0

SECRET_KEY=change_me
APP_ENV=production

# Set by deploy script – written during each deployment
LAST_DEPLOY=
CURRENT_IMAGE=live-cicd-lab:latest
GIT_SHA=local

# Override to deploy specific SHA (set by CI/CD)
APP_IMAGE=live-cicd-lab:latest
